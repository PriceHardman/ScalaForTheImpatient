/*
  Scala has built in functions for, among other operations,
  summing and sorting:
 */
val a = Array(3,1,2,5,4)

a.sum
a.sorted
a.sortBy(_%2==0) // Sort by parity
a.mkString(",") // joining with string separator
a.min
a.max




