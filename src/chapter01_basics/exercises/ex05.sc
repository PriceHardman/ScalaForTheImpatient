/*
  What does 10 max 2 mean? In which class is the max method defined?
 */

10 max 2

// 10 max 2 is actually (10: Int).max(2: Int) and gives us max(10,2) = 10.
// Here, max is a method defined on Int.

(10).max(2)